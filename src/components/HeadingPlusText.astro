---
const { title, text } = Astro.props;
---

<h2 class="component__title">{title}</h2>
{text.map(para => <p class="component__text">{para}</p>)}

<style>

  .component__title {
    border-bottom: var(--heading-bb-dark);
    padding-bottom: 1rem;
  }

  .component__text {
    font-size: 1.2rem;
    font-weight: 300;
    line-height: 1.3;
  }
</style>
